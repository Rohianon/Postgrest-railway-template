# Database connection
# Replace with your actual database connection string
# e.g. postgres://user:password@localhost:5432/dbname
db-uri = "postgres://postgres:postgres@db:5432/postgres"

db-schemas = "public"

db-anon-role = "postgres"

# Server configuration
server-host = "0.0.0.0"
server-port = 3000